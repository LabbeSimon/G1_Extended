<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Bluetooth: pairing and talking to the glasses. -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />

    <!-- Android requires a location permission to scan for BLE devices,
         and the weather widget needs a coarse position. Neither is ever
         sent anywhere except to open-meteo.com for the forecast. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Staying connected while the phone is in a pocket. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <!-- Only for the speedometer, which is off by default. Android will not
         deliver GPS fixes to a backgrounded app without it, and a speed
         readout that stops when the screen locks is worthless. The reading is
         turned into a number on the phone; no position is ever transmitted. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Lets the update button hand a downloaded APK to the system
         installer. The system still confirms every install itself. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Mirroring phone notifications onto the glasses display. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- The dashboard shows the day's agenda on the glasses. -->
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <!-- Declared because device_calendar's permission gate is all or
         nothing: its hasPermissions() answers true only when READ and WRITE
         are both granted, and Android silently refuses any permission the
         manifest does not declare. Without this line the app reported "no
         calendar access" forever, even with read access granted by hand in
         system settings. The app itself never writes an event. -->
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />

    <!-- Dictation and wake word, both recognised on the device. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Reminders are driven by in-process timers, not by the alarm manager,
         so no exact-alarm permission is needed. USE_EXACT_ALARM in particular
         is restricted by Google Play to clock and calendar apps. -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Downloading the offline speech model, and the weather forecast.
         These are the only two things this app ever fetches. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-feature android:name="android.hardware.bluetooth" android:required="false" />
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <!-- largeHeap: the offline speech model is tens of megabytes and its
         loader is native — it allocates outside Dart's heap and cannot be
         caught if it fails. On a standard heap the process was killed from
         the outside with nothing in any log, which is what opening the
         captions screen looked like. This raises the ceiling the system
         enforces. -->
    <application
        android:label="@string/app_name"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:largeHeap="true"
        android:usesCleartextTraffic="false"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Android 14 refuses a foreground service that does not declare its
             type, and kills the process. connectedDevice matches what this
             service does: hold the BLE link to the glasses. -->
        <service
            android:name="fr.simonlabbe.g1extended.BackgroundService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="connectedDevice|location" />

        <service
            android:name="id.flutter.flutter_background_service.BackgroundService"
            android:foregroundServiceType="connectedDevice|location" />

        <service
            android:label="notifications"
            android:name="notification.listener.service.NotificationListener"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="fr.simonlabbe.g1extended.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- The home screen widget. Exported because the launcher, another
             application, is what delivers APPWIDGET_UPDATE to it. -->
        <receiver
            android:name=".GlassesWidgetProvider"
            android:exported="true"
            android:label="G1 Extended">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/glasses_widget_info" />
        </receiver>

        <!-- The actions row, separate from the status tile: one answers how
             the glasses are, the other does the thing. -->
        <receiver
            android:name=".ActionsWidgetProvider"
            android:exported="true"
            android:label="G1 Extended — actions">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/actions_widget_info" />
        </receiver>

        <!-- Receives the widget's button taps and runs the Dart callback for
             them on a background engine, without opening the app. -->
        <receiver
            android:name="es.antonborri.home_widget.HomeWidgetBackgroundReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="es.antonborri.home_widget.action.BACKGROUND" />
            </intent-filter>
        </receiver>
    </application>

    <!-- Used by the Flutter engine's ProcessTextPlugin, and to list the
         installed apps whose notifications can be forwarded to the glasses. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>
</manifest>

<?xml version="1.0" encoding="utf-8"?>
<!-- updatePeriodMillis is zero on purpose: the app pushes an update whenever
     something actually changes, and the system's own schedule would only
     wake the process to redraw identical numbers. -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="250dp"
    android:minHeight="40dp"
    android:targetCellWidth="4"
    android:targetCellHeight="1"
    android:updatePeriodMillis="0"
    android:resizeMode="horizontal"
    android:widgetCategory="home_screen"
    android:initialLayout="@layout/widget_glasses" />
